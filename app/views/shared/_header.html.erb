<nav>
    <%= link_to t('header'), root_path %>
    <%= link_to t('contact'), contact_path %>

    <div class='login' style='float:right;'>
    <% if employee_signed_in? %>
        <%= "#{current_employee.email} |"%>
        <%= link_to t('profile'),edit_employee_registration_path %>
        <%= '|' %>
        <%= link_to t('timesheet'), '/timesheets#index' %>
        <%= '|' %>
        <%= link_to t('logout'),destroy_employee_session_path,method:'delete'%>
    <% else %>
        <%= link_to t('login'),new_employee_session_path %>
        <%= '|' %>
        <%= link_to t('sign_up'),new_employee_registration_path%>
    <% end %>
    </div>
</nav>


<% flash.each do |key, value| %>
      <%= content_tag :div, content_tag(:p, value), id: "#{key}" unless value.blank? %>

    <% end %>