<h1>Shifts</h1>

<table>
  <thead>
    <tr>
      <th>Timesheet</th>
      <th>Date</th>
      <th>Start time</th>
      <th>End time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shiftsindex.each do |shift| %>
      <tr>
        <td><%= link_to shift.timesheet.date_from, @shift.timesheet %></td>
        <td><%= shift.date %></td>
        <td><%= shift.start_time %></td>
        <td><%= shift.end_time %></td>
        <td><%= link_to t('show'), shift %></td>
        <td><%= link_to t('edit'), edit_shift_path(shift) %></td>
        <td><%= link_to t('delete'), shift, method: :delete, data: { confirm: t('confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('.create'), new_shift_path %>
